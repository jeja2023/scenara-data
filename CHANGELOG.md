# 更新日志

## [Unreleased]

- 同步 `@scenara/repository-contracts` `1.0.0` 的当前契约摘要，并将 `DatasetVersionReference` 与 `HardSampleManifest` 的跨仓 `created_at` 统一为以 `Z` 结尾的 UTC RFC3339 字符串。
- Data API 入口继续对 `dataset-version-input` 和 `hard-sample-handoff` 执行严格时间校验；内部领域模型和 PostgreSQL 使用的原生 `datetime/timestamptz` 保持不变。
- 修正独立数据工作台的契约类型、难例清单提交时间和版本引用展示，移除 Unix 秒及 `* 1000` 转换。

## [0.1.4] - 2026-08-17

- 增加独立数据工作台登录页和本地用户名密码会话，登录后由会话令牌访问内部业务 API，避免直连工作台因缺少认证上下文返回 `401`。
- 全面中文化独立数据工作台的品牌、导航、页面标题、字段、状态、错误提示、空状态、设置弹窗和运维信息，统一使用景枢数据产品语言。
- 优化总览、数据集、版本治理、难例导入和运维探针页面的状态反馈、加载失败提示、部分成功展示和移动端响应式布局。
- 修正前端列表请求分页参数，数据集、数据集版本和样本列表统一使用后端契约允许的 `limit=100`，避免因 `limit=200` 被后端以 `422` 拒绝。
- 将就绪探针、健康探针和业务列表请求解耦，业务接口失败时不再把后端状态错误显示为“离线”；设置弹窗测试支持使用草稿连接参数。
- 校正数据集版本状态机操作按钮和反馈文案，避免在不允许的状态下触发构建、校验、发布或归档请求。
- 优化移动端顶部导航、连接状态指示、密集表单、版本表格和导入结果区域，补充文本截断、换行和图标按钮提示。
- 增加前端 API 校验错误摘要，保留后端返回的字段位置和校验原因，便于真实联调排障。
- 增加 `pillow` 与 `cryptography` 开发依赖，修复跨仓库端到端测试对图像库缺失的问题。
- 校准集成测试与 Docker Compose 默认 PostgreSQL 凭据及 CI 环境变量，解决集成测试连接认证失败问题。
- 统一本仓库、前端工作台、锁文件和页面页脚版本号至 `0.1.4`。

## [0.1.3] - 2026-08-16

- 增加独立 Vue 3 + Vite 数据工作台，提供总览、数据集、版本治理、难例导入和运维探针页面。
- 增加独立前端的统一主题、导航壳、连接设置和本机后端联调入口，支持自动端口回退与本机服务探测。
- 增加后端 CORS、前端构建与类型检查、以及面向本机 PostgreSQL/Redis/MinIO 的集成验证入口。
- 补齐前端与部署、测试文档，统一版本号到 0.1.3。

## [0.1.2] - 2026-08-16

- 增加正式 `scenara-contracts` 发布物校验、模式/示例验证和仓库门禁接入。
- 增加跨仓库端到端测试、真实 PostgreSQL/Redis/MinIO 集成冒烟测试、隔离模式迁移执行和本机环境参数化。
- 增加独立 Outbox 工作进程、Core 事件接收端、事件幂等与审计回传。
- 增加迁移导出/导入闭环、包内清单物化、样本与版本成员恢复。

## [0.1.1] - 2026-08-16

- 实现内部数据集、样本、标注、质量、血缘和难例 API，以及 PostgreSQL、S3、Redis 适配器。
- 增加事务内幂等记录、Outbox 领取租约、质量失败报告持久化和租户/项目作用域写入校验。
- 增加迁移包失败报告与终态幂等处理、API 工作流和基础设施自动化测试。
- 将开发 Compose 调整为 PostgreSQL 事实存储模式，并在启动前执行迁移和对象桶初始化。
- 增加正式 `scenara-contracts` 发布物校验、跨仓库端到端测试、独立 Outbox 工作进程，以及真实 PostgreSQL/Redis/MinIO 集成冒烟测试。

## [0.1.0] - 2026-08-15

- 建立 `scenara-data` 独立仓库骨架。
- 增加数据集、数据集版本、样本、标注、清单和对象引用领域模型。
- 增加不可变数据集版本状态转换、领域端口、健康检查和自动测试。
- 锁定 `@scenara/repository-contracts` `1.0.0`，记录难例输入和数据集版本输出依赖。
- 建立 IAM 透传、审计回传、数据库/对象存储、迁移回滚、容量备份恢复和安全责任文档。
