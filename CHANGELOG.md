# 更新日志

## [0.1.9] - 2026-09-05

- **多区域 Tab 切换布局**：全面重构总览页（Overview）、数据集管理（Datasets）、版本治理（Versions）、难例导入（HardSamples）与运维探针（Operations）等包含多区域的页面，统一采用顶部 Tab 选项卡分块切换展示，彻底解决多栏挤压和长垂直流堆叠问题。
- **全界面 UI 深度中文化**：全面审查并中文化页面标题、各级导航、表头字段、操作按钮、技术注释、提示说明与状态徽标，保留机器可读标识的同时补充清晰的业务语言。
- **数据表格高标准规范化**：
  - 单元格具备完整的四周边框，形成严谨规整的网格表格；
  - 统一固定 46px 行高与固定列宽（`table-layout: fixed`），单元格文本强制单行显示，超出自动以省略号截断；
  - 单元格内操作按钮尺寸统一为 28px 高度，垂直居中对齐，绝不撑高单元格行高；
  - 鼠标悬浮于截断单元格时，弹出全局统一的深色精致浮动卡片（`UiTooltip`），彻底弃用浏览器默认 `title` 提示框。
- **难例录入体验重构与极简双模式**：
  - 针对底层契约字段繁杂的问题，新增「极简快速录入」与「完整专家模式」双模式切换；极简模式仅保留核心 5 步业务输入，底层协议参数由系统自动补齐；
  - 专家模式保留全部字段，通过清单元数据、算法纠正与源存储三大子 Tab 进行分步治理；
  - 增设「一键填入演示样例」工具，便于开发与测试快速验证录入流程。
- **时间统一东八区展示**：时间格式化函数强制转换时区为东八区（`Asia/Shanghai`），统一输出不含 `T` 的 `YYYY-MM-DD HH:mm:ss` 标准格式。
- **全局通用组件与样式抽离**：抽象出通用分页组件 `UiPagination`、Tab 切换组件 `UiTabs`、搜索框组件 `UiSearchBox`、自定义浮动提示组件 `UiTooltip` 及对应的数据切片 Composable（`usePagination`、`useTooltip`），并在 `styles.css` 中提炼表格、表单与卡片全局样式规范。
- **页面布局与顶栏精简优化**：移除了顶栏左侧「统一门户 · 独立前端」标签及分隔线，消除了路由通配引起的空白「其他」侧边栏分组；取消居中宽度限制，改为全宽自适应展开，显著缩减宽屏两侧留白。

## [0.1.8] - 2026-09-05

- **上线前演练能力**：新增 PostgreSQL 备份/恢复校验、对象版本快照备份/恢复清单、对象桶版本与校验和巡检、Outbox 死信受控重建、只读 API 并发压测与生产 profile 部署前报告工具。
- **跨仓本地联调**：公开统一的签名身份上下文生成逻辑，并提供 `sign_context.py`，使 Core/Model 可以在不暴露密钥的前提下生成与 Data 服务一致的请求头。
- **证据与门禁**：生产静态门禁要求上述运维工具存在；运维、测试和剩余任务文档增加命令、证据格式与责任边界。

## [0.1.7] - 2026-09-05

- **PostgreSQL 完整性修复**：新增 `0003_production_integrity` 前向迁移，修复已发布数据集版本归档时的 `archived_at` 触发器冲突；统一数据库与领域模型的 `train/validation/test/query/gallery` split 枚举。
- **迁移与难例可靠性**：迁移包恢复版本成员时先在同一事务以 `building` 过渡状态登记成员，再恢复来源终态；非法 UTF-8 和重复 checksum 声明会被明确拒绝。难例清单物化失败会记录为可重试的 `failed`，不再永久滞留 `queued`。
- **访问与工作台加固**：生产配置强制独立、短时签名身份上下文；本地工作台令牌仅保留在当前浏览器会话，并增加登录失败限流。生产默认关闭直连用户名密码登录。
- **部署与质量门禁**：开发 Compose 不再内置数据库/对象存储/服务默认口令，去除浮动 MinIO 标签并开启对象版本控制；新增只连接受管基础设施的生产编排、external secret 模板和生产静态门禁；固定运行时直接依赖版本，CI 补充前端类型检查/构建、依赖漏洞审计与镜像 SBOM/溯源证明。

## [0.1.6] - 2026-09-02

- **契约与模式升级**：消费并严格锁定 `@scenara/repository-contracts` `1.2.0`（清单摘要 `2889523368f6a4d2297b66fc714dfe38d882add7484605c12dcfa3e2e451d111`）与领域标注模式 `domain-annotations` `1.1.0`（清单摘要 `0f2a7ffb271320d5be45a92bf9fc63b08656c33c1983ee01413d7131ed4ba278`）。
- **多领域标注与语义校验**：新增 `annotation_schemas.py` 领域标注模式注册表，支持 Portrait（人体检测、人脸识别、布控误报复核 `scenara.portrait.surveillance-review.v1`）、OCR（文档检测、识别、KIE）、Behavior（骨骼姿态、动作识别）、Fashion（服装风格、属性检测）等领域标注模式的 JSON Schema 规范、支持媒体类型及严格领域语义约束校验。
- **难例交接与布控误报复核支持**：扩展难例交接（`hard-sample-handoff`）数据模型与 API 输入校验，支持 `domain`、`annotation_schema_id` 与结构化 `correction` 复核元数据，扩展数据集拆分类型为 `train`、`validation`、`test`、`query`、`gallery`。
- **数据集版本引用输出增强**：`dataset-version-input`（`DatasetVersionReference`）发布时自动聚合已采纳标注的所属领域 `domain` 与标注模式列表 `annotation_schema_ids`，供模型训练/评估适配器直接消费。
- **独立数据工作台体验升级**：升级 `data-console` 工作台版本至 `0.1.6`；难例导入页面（`HardSamplesView.vue`）支持多领域模式切换、布控复核预置元数据模版、纠错内容快捷配置与 JSON 校验；优化字典标签映射（`labels.ts`、`types.ts`）。
- **契约验证与自动化测试**：新增 `test_domain_annotation_schemas.py` 自动化测试集，更新 `test_core_e2e.py` 适配领域模式与难例交接流程；校准 `scripts/repository_gate.py` 与 `scripts/validate_repository_contracts.py` 严格门禁检查。

## [0.1.5] - 2026-08-22

- 完成四仓库《景枢平台总体开发规范》符合性复核，并在 `docs/REMAINING_TASK_PLAN.md` 记录 Data 仓库剩余任务、生产资格和证据门禁。
- 补齐迁移导入、数据集版本、难例交接、Outbox、探针和独立工作台的当前实现说明，明确真实 PostgreSQL/对象存储/Redis 资格仍需外部环境证据。
- 同步 `scenara-data`、独立 Vue 工作台和 lockfile 到 `0.1.5`，并同步当前版本、部署、开发、API、测试和 README 文档。
- 对齐四仓库契约消费说明，保留历史 `v1.0.0` 契约目录不可变，并切换到兼容性修订版本 `v1.0.1`。
- 本版本验证基线：Data pytest `19 passed, 1 skipped`；跨仓库真实基础设施、容量、恢复和 IAM/Console 外部门禁仍保持 fail-closed。

## [0.1.4] - 2026-08-17

- 增加独立数据工作台登录页和本地用户名密码会话，登录后由会话令牌访问内部业务 API，避免直连工作台因缺少认证上下文返回 `401`。
- 全面中文化独立数据工作台的品牌、导航、页面标题、字段、状态、错误提示、空状态、设置弹窗和运维信息，统一使用景枢数据产品语言。
- 优化总览、数据集、版本治理、难例导入和运维探针页面的状态反馈、加载失败提示、部分成功展示和移动端响应式布局。
- 修正前端列表请求分页参数，数据集、数据集版本和样本列表统一使用后端契约允许的 `limit=100`，避免因 `limit=200` 被后端以 `422` 拒绝。
- 将就绪探针、健康探针和业务列表请求解耦，业务接口失败时不再把后端状态错误显示为“离线”；设置弹窗测试支持使用草稿连接参数。
- 校正数据集版本状态机操作按钮和反馈文案，避免在不允许的状态下触发构建、校验、发布或归档请求。
- 优化移动端顶部导航、连接状态指示、密集表单、版本表格和导入结果区域，补充文本截断、换行和图标按钮提示。
- 增加前端 API 校验错误摘要，保留后端返回的字段位置和校验原因，便于真实联调排障。
- 增加 `pillow` 与 `cryptography` 开发依赖，修复跨仓库端到端测试对图像库缺失的问题。
- 校准集成测试与 Docker Compose 默认 PostgreSQL 凭据及 CI 环境变量，解决集成测试连接认证失败问题。
- 统一本仓库、前端工作台、锁文件和页面页脚版本号至 `0.1.4`。

## [0.1.3] - 2026-08-16

- 增加独立 Vue 3 + Vite 数据工作台，提供总览、数据集、版本治理、难例导入和运维探针页面。
- 增加独立前端的统一主题、导航壳、连接设置和本机后端联调入口，支持自动端口回退与本机服务探测。
- 增加后端 CORS、前端构建与类型检查、以及面向本机 PostgreSQL/Redis/MinIO 的集成验证入口。
- 补齐前端与部署、测试文档，统一版本号到 0.1.3。

## [0.1.2] - 2026-08-16

- 增加正式 `scenara-contracts` 发布物校验、模式/示例验证和仓库门禁接入。
- 增加跨仓库端到端测试、真实 PostgreSQL/Redis/MinIO 集成冒烟测试、隔离模式迁移执行和本机环境参数化。
- 增加独立 Outbox 工作进程、Core 事件接收端、事件幂等与审计回传。
- 增加迁移导出/导入闭环、包内清单物化、样本与版本成员恢复。

## [0.1.1] - 2026-08-16

- 实现内部数据集、样本、标注、质量、血缘和难例 API，以及 PostgreSQL、S3、Redis 适配器。
- 增加事务内幂等记录、Outbox 领取租约、质量失败报告持久化和租户/项目作用域写入校验。
- 增加迁移包失败报告与终态幂等处理、API 工作流和基础设施自动化测试。
- 将开发 Compose 调整为 PostgreSQL 事实存储模式，并在启动前执行迁移和对象桶初始化。
- 增加正式 `scenara-contracts` 发布物校验、跨仓库端到端测试、独立 Outbox 工作进程，以及真实 PostgreSQL/Redis/MinIO 集成冒烟测试。

## [0.1.0] - 2026-08-15

- 建立 `scenara-data` 独立仓库骨架。
- 增加数据集、数据集版本、样本、标注、清单和对象引用领域模型。
- 增加不可变数据集版本状态转换、领域端口、健康检查和自动测试。
- 锁定 `@scenara/repository-contracts` `1.0.0`，记录难例输入和数据集版本输出依赖。
- 建立 IAM 透传、审计回传、数据库/对象存储、迁移回滚、容量备份恢复和安全责任文档。
